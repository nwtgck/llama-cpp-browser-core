{
  "name": "browser-inference-core-source",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "npm --prefix llama-cpp test",
    "test:python": "python3 -m unittest discover -s llama-cpp/tests -p 'test_*.py' && python3 -m unittest discover -s tests -p 'test_*.py' && python3 -m unittest discover -s stable-diffusion-cpp/tests -p 'test_*.py'"
  },
  "engines": {
    "node": ">=22"
  }
}
