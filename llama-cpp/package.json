{
  "name": "llama-cpp-browser-core-source",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Source and build automation, not the installable runtime package",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "test:python": "python3 -m unittest discover -s tests -p 'test_*.py'"
  },
  "engines": { "node": ">=22" }
}
