diff --git a/tools/mtmd/models/models.h b/tools/mtmd/models/models.h
--- a/tools/mtmd/models/models.h
+++ b/tools/mtmd/models/models.h
@@ -245,4 +245,6 @@
 struct clip_graph_qwen3tts_gen : clip_graph {
-    clip_graph_qwen3tts_gen(clip_ctx * ctx, const clip_image_f32 & img, clip_gen_process_type gen_process, int top_k, float top_p)
-        : clip_graph(ctx, img), gen_process(gen_process), top_k(top_k), top_p(top_p) {}
+    clip_graph_qwen3tts_gen(clip_ctx * ctx, const clip_image_f32 & img, clip_gen_process_type gen_process, int top_k, float top_p, int n_frames)
+        : clip_graph(ctx, img), n_frames(n_frames), gen_process(gen_process), top_k(top_k), top_p(top_p) {}
+    // Actual decode length. The attention/state window remains wav_tfm_swa.
+    int n_frames;
     ggml_cgraph * build() override;
diff --git a/tools/mtmd/models/qwen3tts-gen.cpp b/tools/mtmd/models/qwen3tts-gen.cpp
--- a/tools/mtmd/models/qwen3tts-gen.cpp
+++ b/tools/mtmd/models/qwen3tts-gen.cpp
@@ -421,3 +421,3 @@
     const float   kq_scale  = 1.0f / sqrtf((float) d_head);
-    const int64_t W         = hparams.wav_tfm_swa; // == N, frames per batch
+    const int64_t W         = hparams.wav_tfm_swa; // fixed attention/state window
     const int64_t N         = cur->ne[1];
@@ -734,2 +734,2 @@
     // ---- CLIP_GEN_PROCESS_GEN_WAV: 16 RVQ codes -> raw PCM ----
-    const int n_frames = hparams.wav_tfm_swa; // frames per batch, == the attention window
+    GGML_ASSERT(n_frames > 0 && n_frames <= hparams.wav_tfm_swa);
diff --git a/tools/mtmd/clip.cpp b/tools/mtmd/clip.cpp
--- a/tools/mtmd/clip.cpp
+++ b/tools/mtmd/clip.cpp
@@ -1123,3 +1123,12 @@
                 const float top_p = params ? params->top_p : 1.0f;
-                builder = std::make_unique<clip_graph_qwen3tts_gen>(ctx, img, gen_process, top_k, top_p);
+                // Decode only real frames, never a code-0 padded final window.
+                // State capacity and the attention window are unchanged.
+                int n_frames = ctx->model.hparams.wav_tfm_swa;
+                if (gen_process == CLIP_GEN_PROCESS_GEN_WAV && params && params->codes) {
+                    const size_t n_codes = (size_t) ctx->model.gen_code_head_w->ne[2] + 1;
+                    GGML_ASSERT(n_codes > 0 && params->codes->size() % n_codes == 0);
+                    GGML_ASSERT(!params->codes->empty() && params->codes->size() / n_codes <= (size_t) n_frames);
+                    n_frames = (int) (params->codes->size() / n_codes);
+                }
+                builder = std::make_unique<clip_graph_qwen3tts_gen>(ctx, img, gen_process, top_k, top_p, n_frames);
             } break;
@@ -5274,2 +5283,2 @@
-                    // frame-major input to group-major, rear-padded with code 0 up to one window
+                    // Frame-major input to group-major, without synthetic tail frames.
                     const int64_t n_codes  = model.gen_code_head_w->ne[2] + 1;
@@ -5295,5 +5304,5 @@
-                    std::vector<int32_t> codes(n_frames_w * n_codes, 0);
+                    std::vector<int32_t> codes(n_frames * n_codes);
                     for (int64_t f = 0; f < n_frames; f++) {
                         for (int64_t g = 0; g < n_codes; g++) {
-                            codes[g * n_frames_w + f] = (*params->codes)[f * n_codes + g];
+                            codes[g * n_frames + f] = (*params->codes)[f * n_codes + g];
                         }
@@ -5823,9 +5832,3 @@
-        // drop the tail audio that comes from the code-0 rear padding
-        const int64_t n_codes    = params->codes ? model.gen_code_head_w->ne[2] + 1 : 0;
-        const int64_t n_frames_w = hparams.wav_tfm_swa;
-        const int64_t n_frames   = params->codes ? (int64_t) params->codes->size() / n_codes : n_frames_w;
-        if (n_frames < n_frames_w) {
-            const size_t hop = out_audio.size() / n_frames_w;
-            out_audio.resize((size_t) n_frames * hop);
-        }
+        // Qwen decodes the actual frame count; Pocket also returns its exact output.
+
     }
diff --git a/tools/mtmd/mtmd-helper.h b/tools/mtmd/mtmd-helper.h
--- a/tools/mtmd/mtmd-helper.h
+++ b/tools/mtmd/mtmd-helper.h
@@ -216,1 +216,6 @@
+// Whether this loaded pipeline supports lang="auto" (no forced language).
+// False for unknown pipelines or models missing the required conditioning token.
+// Does not run inference and does not claim to classify the input text.
+MTMD_API bool mtmd_helper_gen_audio_supports_language_auto(const mtmd_helper_gen_audio * ctx);
+
 MTMD_API void mtmd_helper_gen_audio_free(mtmd_helper_gen_audio * ctx);
diff --git a/tools/mtmd/mtmd-helper-gen.cpp b/tools/mtmd/mtmd-helper-gen.cpp
--- a/tools/mtmd/mtmd-helper-gen.cpp
+++ b/tools/mtmd/mtmd-helper-gen.cpp
@@ -86,1 +86,2 @@
+    virtual bool supports_language_auto() const { return false; }
     virtual void reset() = 0;
@@ -107,3 +108,7 @@
 class qwen3tts_gen_audio_pipeline : public mtmd_gen_audio_pipeline {
 public:
     using mtmd_gen_audio_pipeline::mtmd_gen_audio_pipeline;
+
+    bool supports_language_auto() const override {
+        return find_special_token(vocab, "<|codec_nothink|>") != LLAMA_TOKEN_NULL;
+    }
@@ -134,4 +139,7 @@
         const std::string lang   = tts_resolve_lang((inp->lang && inp->lang[0]) ? inp->lang : "english");
-        const llama_token c_lang = find_special_token(vocab, ("<|codec_language_" + lang + "|>").c_str());
-        if (c_lang == LLAMA_TOKEN_NULL) {
+        const bool auto_lang = lang == "auto";
+        const llama_token c_lang = auto_lang ? LLAMA_TOKEN_NULL :
+            find_special_token(vocab, ("<|codec_language_" + lang + "|>").c_str());
+        const llama_token conditioning = auto_lang ? find_special_token(vocab, "<|codec_nothink|>") : c_think;
+        if (conditioning == LLAMA_TOKEN_NULL || (!auto_lang && c_lang == LLAMA_TOKEN_NULL)) {
             LOG_ERR("mtmd_helper_gen_audio: unknown language '%s'\n", lang.c_str());
@@ -177,5 +185,7 @@
         for (int i = 0; i < 3; i++) prompt.push_back(row(ids[(size_t) i]));
-        prompt.push_back(sum_row(tts_pad, c_think));
+        // Match Qwen's official auto-language prefix. Do not replace it with
+        // a script heuristic or an English fallback for ambiguous/mixed text.
+        prompt.push_back(sum_row(tts_pad, conditioning));
         prompt.push_back(sum_row(tts_pad, c_think_b));
-        prompt.push_back(sum_row(tts_pad, c_lang));
+        if (!auto_lang) prompt.push_back(sum_row(tts_pad, c_lang));
         prompt.push_back(sum_row(tts_pad, c_think_e));
@@ -1017,1 +1027,5 @@
+bool mtmd_helper_gen_audio_supports_language_auto(const mtmd_helper_gen_audio * ctx) {
+    return ctx && ctx->pipeline && ctx->pipeline->supports_language_auto();
+}
+
 void mtmd_helper_gen_audio_free(mtmd_helper_gen_audio * ctx) {
